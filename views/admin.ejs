<%- include("templates/header") %> 
<div>Users:</div>
<ul>
    <% let i=0; %>
    <% for(i=0; i < users.length; i++) { %>
         <%= users[i].username %>  <%= users[i].user_type == "admin" ? "(admin)" : "(user)" %>
            <form action="/promote/<%= users[i].username %>" method="post" style="display: inline;">
            <button type="submit" class="btn btn-danger btn-sm">Promote</button></form>
            </form>
            <form action="/demote/<%= users[i].username %>" method="post" style="display: inline;">
            <button type="submit" class="btn btn-success btn-sm">Demote</button></form></br></br>
            </form>
        
    <% } %> 
</ul>
<%- include("templates/footer") %> 


